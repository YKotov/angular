import { ActualizationCrsDirective } from './actualization-crs.directive';

describe('ActualizationCrsDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new ActualizationCrsDirective();
    expect(directive).toBeTruthy();
  });
});

