import { CourseLoadDirective } from './course-load.directive';

describe('CourseLoadDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseLoadDirective();
    expect(directive).toBeTruthy();
  });
});
